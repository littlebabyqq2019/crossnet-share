# CrossNetShare v2.1.0 更新日志

发布日期：2026-09-01

## 🎯 主要更新

### 1. ✅ 客户端心跳检测机制（重要）

**问题**：
- 客户端和服务器长时间运行后会出现"假连接"状态
- 客户端显示已连接，但服务器实际已断开该连接
- Web 界面无法看到客户端，无法检索文件

**解决方案**：
- 添加客户端心跳机制，每 30 秒发送一次心跳
- 每 60 秒检查一次心跳响应
- 如果 60 秒内没有收到响应，主动断开并自动重连
- 确保连接状态的实时准确性

**技术细节**：
```cpp
// 心跳定时器 - 每 30 秒发送
heartbeatTimer_->setInterval(30000);

// 心跳检查定时器 - 每 60 秒检查
heartbeatCheckTimer_->setInterval(60000);

// 如果超时，强制重连
if (secondsSinceLastSent > 60) {
    socket_->disconnectFromHost();
    // 触发自动重连机制
}
```

**受影响的文件**：
- `client/client.h` - 添加心跳相关成员变量和方法
- `client/client.cpp` - 实现心跳发送、检查和响应处理
- `server/client_handler.cpp` - 服务器已有心跳响应（无需修改）

### 2. ✅ 索引文件位置优化

**更改**：
- 索引文件从 `%LOCALAPPDATA%\CrossNetShareClient\` 移动到客户端程序目录
- 更容易找到和管理索引文件

**新位置**：
```
<客户端.exe所在目录>\content_index.db
```

**优点**：
- 与程序在一起，一目了然
- 便于备份和移植
- 方便调试和监控

详见：`INDEX_PATH_CHANGE.md`

## 🔧 技术改进

### 心跳机制流程

```
客户端启动
    ↓
连接服务器成功
    ↓
启动心跳定时器（30秒间隔）
启动心跳检查定时器（60秒间隔）
    ↓
每30秒：发送心跳 → 等待响应
    ↓
收到响应：重置等待标志 ✓
    ↓
每60秒：检查是否收到响应
    ↓
超过60秒未响应：强制断开 → 自动重连 ✓
```

### 自动重连机制（已有，优化）

```
断开连接
    ↓
检查 autoReconnect_ 标志
    ↓
是：延迟重连（3秒 → 6秒 → 9秒 ... 最多30秒）
    ↓
重连成功：重置重试次数，恢复心跳
    ↓
重连失败：继续重试
```

## 📊 性能影响

### 心跳开销
- **网络流量**：每次心跳约 100-200 字节
- **频率**：每 30 秒一次
- **每小时流量**：约 12-24 KB（可忽略）
- **CPU 占用**：< 0.01%（几乎无影响）

### 重连时间
- **检测延迟**：最多 60 秒（心跳检查间隔）
- **重连延迟**：3-30 秒（指数退避）
- **总延迟**：最多 90 秒从断开到恢复

## 🐛 已修复问题

### 问题 1：假连接状态
- **症状**：客户端显示已连接，但服务器认为未连接
- **原因**：网络波动、NAT 超时、服务器重启等导致静默断开
- **修复**：心跳机制主动检测并重连

### 问题 2：Web 界面看不到客户端
- **症状**：客户端下拉框为空，无法检索
- **原因**：服务器认为客户端已断开
- **修复**：心跳确保连接有效性

### 问题 3：长时间运行后失效
- **症状**：程序运行几小时后功能失效
- **原因**：TCP 连接静默失效
- **修复**：定期心跳保持连接活跃

## 📝 使用说明

### 自动功能（无需配置）
- 心跳机制**自动启用**，无需配置
- 自动重连**默认启用**（可通过配置禁用）
- 索引文件**自动保存**在程序目录

### 日志输出
连接时会看到：
```
Connected to server - heartbeat started
```

心跳超时时会看到：
```
Heartbeat timeout - connection appears dead, forcing reconnect...
Disconnected from server
Will attempt to reconnect in 3 seconds...
```

### 监控连接状态
心跳是静默的（不输出日志），但可以通过以下方式监控：
1. 查看客户端窗口状态栏
2. 查看 Web 界面的客户端列表
3. 尝试搜索文件验证功能

## 🔄 升级指南

### 从 v2.0.x 升级到 v2.1.0

**步骤**：
1. 停止旧版本客户端和服务器
2. 替换可执行文件
3. 重新启动服务器和客户端
4. 验证连接和心跳功能

**兼容性**：
- ✅ **完全向后兼容**
- ✅ 旧版本客户端可以连接新版本服务器（无心跳）
- ✅ 新版本客户端可以连接旧版本服务器（心跳会失败但不影响功能）
- ✅ 配置文件格式不变

**索引文件迁移**（可选）：
```powershell
# 如果想保留旧索引，手动复制
$oldPath = "$env:LOCALAPPDATA\CrossNetShareClient\content_index.db"
$newPath = "<客户端目录>\content_index.db"
Copy-Item $oldPath $newPath
```

如果不迁移，会自动创建新索引（3-10 分钟）。

## 🧪 测试建议

### 测试 1：心跳功能
1. 启动客户端并连接服务器
2. 查看日志：应该看到 "Connected to server - heartbeat started"
3. 等待几分钟，连接应该保持稳定

### 测试 2：自动重连
1. 客户端连接后，停止服务器
2. 客户端应该显示 "Disconnected from server"
3. 重启服务器
4. 客户端应该自动重连并显示 "Connected to server - heartbeat started"

### 测试 3：心跳超时重连
1. 使用防火墙阻止客户端和服务器之间的通信（但不断开连接）
2. 等待 60-90 秒
3. 客户端应该检测到心跳超时并强制重连

### 测试 4：Web 界面稳定性
1. 启动客户端和服务器
2. 在 Web 界面 (http://localhost:8080/browse) 验证客户端可见
3. 运行几小时后，再次检查客户端是否仍然可见
4. 尝试搜索文件，验证功能正常

## 📚 相关文档

- `INDEX_PATH_CHANGE.md` - 索引文件位置变更详情
- `FIND_INDEX_GUIDE.md` - 如何找到索引文件
- `INDEX_PERFORMANCE_FAQ.md` - 索引性能和实时更新说明

## 🔜 未来计划

- [ ] Web 界面显示客户端心跳状态
- [ ] 可配置的心跳间隔
- [ ] 心跳统计和监控面板
- [ ] 更智能的重连策略（基于网络状况）

## 📞 反馈

如果遇到问题：
1. 检查客户端日志输出
2. 查看服务器日志
3. 验证网络连接
4. 提交 Issue 并附上日志

## 版本信息

- **版本号**：2.1.0
- **发布日期**：2026-09-01
- **主要改进**：客户端心跳检测和自动重连
- **兼容性**：向后兼容 v2.0.x
