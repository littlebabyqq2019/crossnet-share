# CrossNetShare v2.2.3 更新日志

**发布日期**: 2026-09-04

## 🐛 Bug 修复

### 修复电脑休眠后客户端假连接问题

**问题描述**:
- 电脑休眠一晚上后唤醒，客户端虽然显示"已连接"，但服务端显示0个在线客户端
- 客户端重连并注册成功，但约43秒后服务端检测到"The remote host closed the connection"
- 需要手动点击服务端"刷新文件索引"按钮才能恢复正常连接
- 根本原因：休眠后TCP连接处于"半死"状态，客户端认为已连接，服务端认为已断开

**修复方案**:

1. **重连后立即验证连接**
   - 在 `onConnected()` 后1秒立即发送初始心跳验证连接有效性
   - 特别针对休眠后重连的场景，快速检测"半死"连接

2. **缩短心跳检查间隔**
   - 检查间隔从60秒改为30秒（与发送间隔一致）
   - 更快地检测到连接问题并触发重连

3. **更敏感的超时检测**
   - 超时判定从60秒改为45秒（留出15秒缓冲）
   - 添加额外检查：如果长时间（>60秒）没有发送心跳，主动发送验证连接
   - 输出详细的超时时长日志，便于诊断

**修改文件**:
- `client/client.cpp`:
  - `onConnected()`: 添加初始心跳发送
  - `heartbeatCheckTimer_`: 间隔改为30秒
  - `checkHeartbeatResponse()`: 超时改为45秒，添加额外检查

**技术细节**:

### TCP 连接的"半死"状态

当电脑休眠时：
1. TCP 连接的内核状态被冻结
2. 唤醒后，客户端的套接字仍然认为处于 ESTABLISHED 状态
3. 但服务端可能已经超时关闭了连接，或网络路由已经失效
4. 这种状态下 `socket->state() == ConnectedState`，但实际无法通信

### 心跳机制的改进

**之前的时间配置**:
```
发送间隔: 30秒
检查间隔: 60秒
超时判定: 60秒
```

问题：检查间隔太长，第一次检查要等60秒，此时可能已经错过了多个超时。

**改进后的配置**:
```
发送间隔: 30秒
检查间隔: 30秒（改进）
超时判定: 45秒（改进）
初始验证: 重连后1秒（新增）
```

优势：
- 重连后快速验证（1秒）
- 更频繁的检查（30秒）
- 更快的超时响应（45秒）
- 额外的安全检查（60秒无心跳时主动发送）

### 时间轴示例（休眠唤醒场景）

**改进前**:
```
00:00 - 客户端重连成功
00:01 - 注册成功
00:30 - 第一个心跳发送（可能失败，但不知道）
01:00 - 第一次检查，发现超时
01:00 - 强制断开重连
```
问题：需要60秒才能检测到连接失效

**改进后**:
```
00:00 - 客户端重连成功
00:01 - 初始心跳发送（立即验证）
00:01 - 如果无响应，30秒后检测到
00:31 - 第一次检查，发现超时（30秒）
00:31 - 强制断开重连
```
优势：最快30秒（最坏情况45秒）检测到连接问题

## 用户体验改进

**之前**:
- 休眠唤醒后客户端显示已连接，但实际无法使用
- 需要手动操作（点击服务端按钮）才能恢复
- 用户困惑："为什么显示已连接却不工作？"

**改进后**:
- 休眠唤醒后自动检测连接失效
- 30-45秒内自动重连并恢复正常
- 日志显示详细的检测和重连过程
- 无需手动干预

## 测试建议

1. **正常运行测试**：
   - 启动客户端，观察初始心跳日志："Initial heartbeat sent to verify connection"
   - 确认客户端和服务端都显示已连接

2. **休眠唤醒测试**：
   - 让客户端保持连接状态
   - 电脑进入休眠（至少5分钟）
   - 唤醒电脑
   - 观察日志：
     - 应该看到 "Heartbeat timeout" 或 "Warning: No heartbeat sent"
     - 然后看到 "Reconnecting to server"
     - 最后看到 "Connected to server" 和 "Registration successful"
   - 确认服务端显示客户端在线

3. **长时间运行测试**：
   - 让程序运行一整天
   - 观察心跳日志是否正常
   - 确认没有异常断开

## 升级说明

直接替换 `CrossNetShareClient.exe` 即可，无需删除索引文件或修改配置。

## 相关问题

此修复解决了以下场景的连接问题：
- 电脑休眠/睡眠后唤醒
- 网络暂时中断后恢复
- 服务端重启后客户端自动重连
- 长时间运行后的连接维护

---

**版本**: v2.2.3
**上一版本**: v2.2.2
**提交**: (待生成)
