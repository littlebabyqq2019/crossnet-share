# CrossNetShare v2.2.4 更新日志

**发布日期**: 2026-09-04

## ✨ 用户体验改进

### 静默启动 - 自动最小化到系统托盘

**改进内容**:
- ✅ 开机自动运行后，客户端和服务端自动最小化到系统托盘
- ✅ 取消所有托盘通知消息，实现静默启动
- ✅ 关闭主窗口时也不再显示提示消息

**修改详情**:

#### 客户端改进
1. **启动后自动隐藏**
   - 启动1秒后自动隐藏到系统托盘
   - 移除 "客户端已启动，运行在系统托盘中" 提示
   - 移除 "窗口将保持可见以便查看日志" 调试提示

2. **关闭窗口时静默**
   - 点击关闭按钮时隐藏到托盘
   - 移除 "程序已最小化到系统托盘" 提示

#### 服务端改进
1. **启动后自动隐藏**
   - 启动0.5秒后自动启动服务器并隐藏到托盘
   - 移除 "服务器已启动，运行在系统托盘中" 提示

2. **关闭窗口时静默**
   - 点击关闭按钮时隐藏到托盘
   - 移除 "程序已最小化到系统托盘" 提示

**用户体验**:

**改进前**:
```
1. 开机启动
2. 显示主窗口
3. 弹出通知："程序已启动，运行在系统托盘中"
4. 窗口最小化到托盘
5. 点击关闭按钮
6. 弹出通知："程序已最小化到系统托盘"
```
→ 频繁的通知打扰用户

**改进后**:
```
1. 开机启动
2. 短暂显示主窗口（<1秒）
3. 自动隐藏到系统托盘
4. 无任何通知消息
5. 点击关闭按钮
6. 静默隐藏到托盘
```
→ 完全静默，不打扰用户

**访问方式**:

托盘图标功能保持不变：
- **双击托盘图标**: 显示主窗口
- **右键托盘图标**: 显示菜单
  - "显示主窗口": 打开主窗口
  - "退出": 完全退出程序

**配置文件**:

无需任何配置文件修改，静默启动是默认行为。

## 适用场景

此改进特别适合：
1. **办公环境**: 开机自动启动，不打扰工作
2. **服务器场景**: 后台静默运行，无需人工干预
3. **家庭用户**: 开机即用，无感知运行
4. **多用户环境**: 减少通知干扰

## 技术实现

### 客户端 (client/ui/main_window.cpp)

```cpp
void MainWindow::setupTrayIcon() {
    // ... 创建托盘图标 ...
    
    // 启动后自动隐藏到托盘（静默启动）
    QTimer::singleShot(1000, this, [this]() {
        hide();
        // 不显示任何提示消息，静默运行
    });
}

void MainWindow::closeEvent(QCloseEvent* event) {
    if (trayIcon_ && trayIcon_->isVisible()) {
        hide();
        event->ignore();
        // 不显示最小化提示消息
    } else {
        event->accept();
    }
}
```

### 服务端 (server/ui/main_window.cpp)

```cpp
// 构造函数中
QTimer::singleShot(500, this, [this]() {
    if (!server_->isRunning()) {
        onStartStopClicked();
    }
    // 启动后隐藏到托盘，不显示任何提示消息
    hide();
});

void MainWindow::closeEvent(QCloseEvent* event) {
    if (trayIcon_ && trayIcon_->isVisible()) {
        hide();
        event->ignore();
        // 不显示最小化提示消息
    } else {
        event->accept();
    }
}
```

## 注意事项

### 首次使用提示

首次使用的用户可能不知道程序已经启动，建议：
1. 查看系统托盘区域
2. 寻找 CrossNetShare 图标
   - 客户端：网络驱动器图标
   - 服务端：计算机图标
3. 双击图标打开主窗口

### 调试模式

如果需要查看启动过程中的日志：
1. 从开始菜单启动（不使用开机自动运行）
2. 或临时注释掉 `hide()` 调用
3. 或检查日志文件：
   - 客户端：`client_log.txt`（如果已启用）
   - 服务端：控制台输出

### 系统托盘支持

某些 Linux 桌面环境可能不支持系统托盘：
- GNOME 需要安装扩展
- KDE 默认支持
- XFCE 默认支持
- 如果托盘不可用，程序将正常显示窗口

## 修改文件

- `client/ui/main_window.cpp`: 客户端静默启动
- `server/ui/main_window.cpp`: 服务端静默启动
- `CMakeLists.txt`: 版本号升级到 2.2.4

## 升级说明

直接替换可执行文件即可：
- `CrossNetShareClient.exe`
- `CrossNetShareServer.exe`

无需修改配置文件或删除数据。

## 向后兼容

此改进完全向后兼容：
- 不影响现有功能
- 配置文件格式不变
- 数据库结构不变
- 只是改进了用户界面行为

---

**版本**: v2.2.4
**上一版本**: v2.2.3
**提交**: (待生成)
