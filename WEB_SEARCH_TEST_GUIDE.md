# Web 全文搜索测试指南

## 测试环境准备

### 1. 确保服务正在运行
- 服务端正在运行（端口 8080）
- 至少一个客户端已连接并索引了文件

### 2. 浏览器访问
打开浏览器访问：http://localhost:8080/browse

## 测试场景

### 场景 1：文件名实时过滤（原有功能）
**目的**：验证输入时的实时文件名过滤仍然工作

1. 在搜索框输入部分文件名（例如："doc"）
2. **预期结果**：立即显示包含"doc"的文件（文件名或路径）
3. 清空搜索框
4. **预期结果**：显示所有文件

✅ 通过标准：输入时立即过滤，无需按回车或点击按钮

### 场景 2：全文搜索 - 点击按钮
**目的**：验证点击"全文搜索"按钮触发内容搜索

1. 在搜索框输入中文关键词（例如："雁塔"）
2. 点击绿色"全文搜索"按钮
3. **预期结果**：
   - 按钮变为"搜索中..."并禁用
   - 3 秒内返回结果
   - 找到的文件显示黄色背景高亮
   - 文件名旁显示绿色"全文"徽章
   - 计数显示"X 个全文搜索结果"

✅ 通过标准：
- 找到包含"雁塔"内容的文件（即使文件名不包含"雁塔"）
- 结果有明显的视觉区分（黄色高亮 + 徽章）

### 场景 3：全文搜索 - 回车键
**目的**：验证回车键快捷方式

1. 在搜索框输入关键词（例如："中国"）
2. 按回车键
3. **预期结果**：与场景 2 相同的搜索行为

✅ 通过标准：回车键和点击按钮效果一致

### 场景 4：布尔搜索 - OR 运算符
**目的**：验证布尔运算符支持

1. 在搜索框输入：`"雁塔 or 中国"`
2. 点击"全文搜索"或按回车
3. **预期结果**：
   - 返回包含"雁塔"或"中国"的所有文件
   - 结果数量 >= 单独搜索"雁塔"或"中国"的数量

✅ 通过标准：OR 搜索返回更多结果（合集）

### 场景 5：清空搜索返回文件列表
**目的**：验证退出全文搜索模式

1. 执行全文搜索（例如："雁塔"）
2. 查看搜索结果（黄色高亮）
3. 清空搜索框（删除所有文本）
4. **预期结果**：
   - 自动退出全文搜索模式
   - 显示所有文件
   - 黄色高亮消失

✅ 通过标准：清空输入立即返回文件列表

### 场景 6：客户端过滤在全文搜索模式下工作
**目的**：验证客户端过滤器与全文搜索的协同

1. 执行全文搜索（例如："西安"）
2. 在"客户端过滤"下拉框选择特定客户端
3. **预期结果**：
   - 只显示该客户端的全文搜索结果
   - 保持黄色高亮和"全文"徽章

✅ 通过标准：客户端过滤应用到全文搜索结果

### 场景 7：英文搜索
**目的**：验证英文关键词搜索

1. 在搜索框输入英文关键词（例如："cos"）
2. 点击"全文搜索"
3. **预期结果**：返回包含"cos"的文件

✅ 通过标准：英文搜索正常工作

### 场景 8：空查询提示
**目的**：验证空查询处理

1. 清空搜索框
2. 点击"全文搜索"按钮
3. **预期结果**：弹出提示"请输入搜索关键词"

✅ 通过标准：有友好的错误提示

### 场景 9：混合使用文件名过滤和全文搜索
**目的**：验证两种搜索模式的切换

1. 输入关键词进行文件名过滤
2. 按回车触发全文搜索
3. 修改关键词（输入事件）
4. **预期结果**：
   - 步骤 1：显示文件名过滤结果
   - 步骤 2：切换到全文搜索结果（黄色高亮）
   - 步骤 3：如果清空，返回文件列表；如果继续输入，保持全文搜索结果但实时文件名过滤

注意：当前实现是输入时如果清空则退出全文搜索，否则保持全文搜索结果。

✅ 通过标准：两种模式平滑切换

## PowerShell API 测试

使用提供的测试脚本：

```powershell
.\test_web_search.ps1
```

预期输出：
```
=== CrossNetShare Web 搜索测试 ===

步骤 1: 登录...
✓ 登录成功

步骤 2: 测试搜索 '雁塔'...
✓ 搜索成功
查询: 雁塔
结果数量: 4
找到的文件:
  - 文件1.docx (来自: Client-001)
  - 文件2.doc (来自: Client-001)
  ...

步骤 3: 测试 OR 搜索 '雁塔 or 中国'...
✓ 搜索成功
结果数量: 6

步骤 4: 测试英文搜索 'cos'...
✓ 搜索成功
```

## 浏览器开发者工具检查

### 检查网络请求
1. 打开浏览器开发者工具（F12）
2. 切换到 Network 标签
3. 执行全文搜索
4. 查看 `/api/content-search` 请求：

**请求**：
```http
POST /api/content-search HTTP/1.1
Content-Type: application/json

{"query":"雁塔"}
```

**响应**：
```json
{
  "success": true,
  "query": "雁塔",
  "totalResults": 4,
  "results": [
    {
      "filename": "文件1.docx",
      "relativePath": "Documents/文件1.docx",
      "ownerClient": "Client-001",
      "size": 12345,
      "modifyTime": "2026-08-25 10:30:00"
    }
  ]
}
```

### 检查控制台错误
- 不应该有 JavaScript 错误
- 不应该有网络请求失败

## 常见问题排查

### 问题 1：搜索返回 0 结果
**可能原因**：
1. 客户端未连接
2. 文件未索引
3. 搜索关键词确实不存在

**排查步骤**：
1. 检查服务端日志，确认收到搜索请求
2. 检查客户端日志，确认处理搜索请求
3. 尝试搜索已知存在的关键词

### 问题 2：搜索按钮点击无响应
**可能原因**：
1. JavaScript 错误
2. 网络问题

**排查步骤**：
1. 打开浏览器控制台查看错误
2. 检查网络标签是否有请求发出
3. 刷新页面重试

### 问题 3：中文显示为乱码（"??"）
**可能原因**：
- 编码问题（这是已知问题，但不影响搜索功能）

**说明**：
- PowerShell 测试中可能出现编码问题
- Web 界面应该正常显示中文
- 如果 Web 界面也乱码，检查浏览器编码设置（应为 UTF-8）

### 问题 4：全文搜索很慢
**可能原因**：
1. 客户端数量多
2. 文件数量大
3. 网络延迟

**说明**：
- 当前超时设置为 3 秒
- 可以在 `handleContentSearch()` 中调整超时时间

## 成功标准总结

✅ **核心功能**：
- 文件名实时过滤正常工作
- 全文搜索能找到内容匹配的文件
- 回车键和按钮都能触发搜索

✅ **用户体验**：
- 全文搜索结果有明显视觉区分（黄色高亮 + 徽章）
- 按钮状态反馈清晰（搜索中...）
- 清空搜索自然返回文件列表

✅ **高级功能**：
- 布尔运算符（OR, AND, NOT）工作正常
- 客户端过滤器与全文搜索协同
- 中文和英文搜索都支持

## 测试报告模板

```
测试日期：____________________
测试人员：____________________

场景 1 - 文件名过滤：[ ] 通过 [ ] 失败
场景 2 - 全文搜索按钮：[ ] 通过 [ ] 失败
场景 3 - 回车键搜索：[ ] 通过 [ ] 失败
场景 4 - OR 运算符：[ ] 通过 [ ] 失败
场景 5 - 清空返回：[ ] 通过 [ ] 失败
场景 6 - 客户端过滤：[ ] 通过 [ ] 失败
场景 7 - 英文搜索：[ ] 通过 [ ] 失败
场景 8 - 空查询提示：[ ] 通过 [ ] 失败
场景 9 - 混合使用：[ ] 通过 [ ] 失败

问题记录：
_________________________________________________
_________________________________________________

总体评价：[ ] 优秀 [ ] 良好 [ ] 需改进
```

## 下一步建议

如果所有测试通过，可以考虑：
1. 添加搜索历史记录功能
2. 高亮显示匹配文本片段
3. 添加搜索结果排序选项
4. 优化搜索性能（如果文件数量很大）
