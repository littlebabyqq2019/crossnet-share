# CrossNetShare 安装指南

## 目录

1. [系统要求](#系统要求)
2. [下载所需文件](#下载所需文件)
3. [Python 环境配置](#python-环境配置)
4. [Aspose.Words 安装](#asposewords-安装)
5. [许可证配置](#许可证配置)
6. [验证安装](#验证安装)
7. [故障排除](#故障排除)

---

## 系统要求

### 硬件要求
- CPU: 双核 2.0 GHz 或更高
- 内存: 4GB RAM（推荐 8GB）
- 硬盘: 500MB 可用空间（不含 Python）

### 软件要求
- **操作系统**: Windows 10/11（64位）
- **Python**: 3.8 或更高版本（推荐 3.11/3.12/3.13）
- **.NET Framework**: 无需安装（Qt 自带运行库）

### 网络要求
- 服务器端: 开放端口 8888（文件传输）和 8080（Web 访问）
- 客户端: 能够访问服务器的 IP 地址

---

## 下载所需文件

### 文件清单

安装 CrossNetShare 需要以下文件：

1. ✅ **CrossNetShare 程序**（从 GitHub 下载）
2. ✅ **Aspose.Words whl 包**（单独下载）
3. ✅ **许可证文件**（单独下载）

---

### 1. 下载 CrossNetShare 程序

**下载地址:**
- GitHub Releases: https://github.com/littlebabyqq2019/crossnet-share/releases
- 下载文件: `CrossNetShare-Windows-x64.zip`

**解压后目录结构:**
```
CrossNetShare-Windows-x64/
├── Server/              ← 服务器端程序
│   ├── CrossNetShareServer.exe
│   ├── word_to_pdf.py   ← Python 转换脚本
│   └── *.dll           ← Qt 运行库
├── Client/              ← 客户端程序
│   ├── CrossNetShareClient.exe
│   └── *.dll
├── README.md
└── QUICKSTART.md
```

⚠️ **重要提示:** GitHub 编译包不包含 Aspose.Words 和许可证文件，需要单独下载。

---

### 2. 下载 Aspose.Words 包

**下载来源:**
- GitCode: https://gitcode.com/qq_56222266/cracked_aspose
- 下载 `Python专用whl包.zip`

**或者从项目维护者处获取完整的 Aspose.Words 文件夹**

**需要的文件:**
```
Aspose.Words/
└── python专用whl包/
    ├── aspose_words-25.9.0-py3-none-win_amd64.whl  ← Windows 64位（推荐）
    ├── aspose_words-25.9.0-py3-none-win32.whl      ← Windows 32位
    ├── aspose_words-25.9.0-py3-none-macosx_*.whl   ← macOS
    ├── aspose_words-25.9.0-py3-none-manylinux*.whl ← Linux
    └── 1.lic                                        ← 许可证文件（重要！）
```

**文件说明:**
- `aspose_words-*.whl` - Python 安装包（选择对应平台）
- `1.lic` - 许可证文件（用于去除水印）

---

### 3. 文件组织建议

**推荐目录结构:**
```
C:\CrossNetShare\
├── Server\                    ← CrossNetShare 服务器
│   ├── CrossNetShareServer.exe
│   ├── word_to_pdf.py
│   ├── 1.lic                  ← 将许可证复制到这里
│   └── *.dll
├── Client\                    ← CrossNetShare 客户端
│   ├── CrossNetShareClient.exe
│   └── *.dll
└── Aspose.Words\              ← Aspose 文件（用于安装）
    └── python专用whl包\
        ├── aspose_words-25.9.0-py3-none-win_amd64.whl
        └── 1.lic
```

---

## Python 环境配置

### 方式一：全新安装 Python（推荐）

#### 1. 下载 Python

**下载地址:**
- 官网: https://www.python.org/downloads/
- 推荐版本: **Python 3.12.x** 或 **Python 3.13.x**

**下载文件示例:**
- `python-3.12.7-amd64.exe` （Windows 64位）
- `python-3.13.2-amd64.exe` （Windows 64位，最新版）

#### 2. 安装 Python

**⚠️ 关键步骤 - 请仔细阅读！**

1. **双击运行安装程序**

2. **在安装界面第一步:**
   ```
   ┌────────────────────────────────────────────┐
   │  Install Python 3.12.x (64-bit)            │
   │                                            │
   │  ☑ Add Python to PATH  ← 必须勾选！！！   │
   │  ☐ Use admin privileges when installing... │
   │                                            │
   │  [Install Now]  [Customize Installation]   │
   └────────────────────────────────────────────┘
   ```

   **必须操作:**
   - ✅ **勾选 "Add Python to PATH"** ← 非常重要！
   - ✅ 推荐勾选 "Install for all users"（如果有管理员权限）
   - ❌ 不要勾选 "Use admin privileges when installing py.exe"

3. **点击 "Install Now"** 进行快速安装
   - 或选择 "Customize Installation" 进行自定义（高级用户）

4. **等待安装完成**
   - 看到 "Setup was successful" 表示安装成功
   - 点击 "Close" 关闭安装程序

#### 3. 验证 Python 安装

**打开命令提示符:**
- 按 `Win + R`
- 输入 `cmd`
- 按回车

**验证命令:**
```cmd
python --version
```

**预期输出:**
```
Python 3.12.7
```

**如果显示错误:**
```
'python' 不是内部或外部命令
```

说明 Python 未添加到 PATH，需要：
1. 卸载 Python
2. 重新安装并确保勾选 "Add Python to PATH"

---

### 方式二：已安装 Python

如果电脑已经安装了 Python：

**检查版本:**
```cmd
python --version
```

**版本要求:**
- ✅ Python 3.8 或更高 - 可以使用
- ❌ Python 3.7 或更低 - 必须升级
- ❌ Python 2.x - 不支持，必须安装 Python 3

---

## Aspose.Words 安装

### 安装步骤

#### 1. 定位 whl 文件

确认你已下载 Aspose.Words whl 包，并知道其路径。

**示例路径:**
```
C:\CrossNetShare\Aspose.Words\python专用whl包\
```

#### 2. 打开命令提示符

按 `Win + R`，输入 `cmd`，按回车

#### 3. 切换到 whl 文件所在目录

```cmd
cd /d C:\CrossNetShare\Aspose.Words\python专用whl包
```

**注意:** 替换为你的实际路径

#### 4. 安装 whl 包

**Windows 64位系统（推荐）:**
```cmd
pip install aspose_words-25.9.0-py3-none-win_amd64.whl --force-reinstall
```

**Windows 32位系统:**
```cmd
pip install aspose_words-25.9.0-py3-none-win32.whl --force-reinstall
```

**参数说明:**
- `--force-reinstall` - 强制重新安装，覆盖旧版本
- 如果提示权限错误，添加 `--user` 参数

#### 5. 等待安装完成

**成功输出示例:**
```
Processing c:\crossnetshare\aspose.words\python专用whl包\aspose_words-25.9.0-py3-none-win_amd64.whl
Installing collected packages: aspose-words
Successfully installed aspose-words-25.9.0
```

#### 6. 验证安装

```cmd
python -c "import aspose.words as aw; print('✓ Aspose.Words', aw.__version__)"
```

**预期输出:**
```
✓ Aspose.Words 25.9.0
```

**如果出错:**
- 检查 Python 是否正确安装
- 检查 whl 文件路径是否正确
- 检查 whl 文件是否与系统匹配（64位/32位）

---

## 许可证配置

### 为什么需要许可证？

- ❌ **无许可证**: 生成的 PDF 会有 "Evaluation Only" 水印
- ✅ **有许可证**: 无水印，完整功能

### 配置步骤

#### 1. 找到许可证文件

许可证文件位置:
```
Aspose.Words\python专用whl包\1.lic
```

#### 2. 复制许可证到服务器目录

**使用命令提示符:**
```cmd
copy "C:\CrossNetShare\Aspose.Words\python专用whl包\1.lic" "C:\CrossNetShare\Server\1.lic"
```

**或手动复制:**
1. 打开 `Aspose.Words\python专用whl包\` 文件夹
2. 找到 `1.lic` 文件
3. 复制该文件
4. 粘贴到 `Server\` 目录（与 CrossNetShareServer.exe 同目录）

#### 3. 验证许可证位置

**最终目录结构:**
```
Server\
├── CrossNetShareServer.exe
├── word_to_pdf.py
├── 1.lic                    ← 许可证文件必须在这里
└── *.dll
```

### 许可证查找顺序

程序启动时会按以下顺序查找许可证:

1. `<服务器目录>\1.lic` ← **推荐位置**
2. `<服务器目录>\aspose.words.lic`
3. `<服务器目录>\aspose.lic`
4. 开发环境路径（仅用于开发调试）

只要其中一个位置有许可证文件即可。

---

## 验证安装

### 完整验证流程

#### 步骤 1: 验证 Python

```cmd
python --version
```
**期望:** `Python 3.8+`

---

#### 步骤 2: 验证 Aspose.Words

```cmd
python -c "import aspose.words; print('✓ Aspose.Words installed successfully')"
```
**期望:** `✓ Aspose.Words installed successfully`

---

#### 步骤 3: 验证许可证

```cmd
dir Server\1.lic
```
**期望:** 显示文件信息（大小约 1KB）

---

#### 步骤 4: 测试转换（可选）

创建测试脚本 `test_aspose.py`:

```python
import aspose.words as aw
import os

print("=" * 60)
print("Testing Aspose.Words Installation")
print("=" * 60)

# 测试许可证加载
license_file = "Server/1.lic"
if os.path.exists(license_file):
    try:
        lic = aw.License()
        lic.set_license(license_file)
        print("✓ License loaded successfully from:", license_file)
    except Exception as e:
        print("✗ Failed to load license:", e)
else:
    print("✗ License file not found:", license_file)

print("=" * 60)
print("Installation test complete!")
```

运行测试:
```cmd
python test_aspose.py
```

---

#### 步骤 5: 启动服务器

```cmd
cd Server
CrossNetShareServer.exe
```

**查看日志输出:**

**成功标志:**
```
[日期 时间] Server started successfully
[日期 时间] Found Aspose license at: C:\CrossNetShare\Server\1.lic
```

**失败标志:**
```
[日期 时间] No Aspose license found, will run in evaluation mode
```

如果看到失败标志，请检查许可证文件位置。

---

#### 步骤 6: 测试 Word 预览

1. 启动客户端: `Client\CrossNetShareClient.exe`
2. 连接到服务器
3. 或打开浏览器访问: `http://服务器IP:8080`
4. 登录后打开一个 .doc 或 .docx 文件
5. 查看 PDF 预览

**成功标志:**
- ✅ 预览加载速度快（1-3秒）
- ✅ PDF 清晰无水印
- ✅ 服务器日志显示: `✓ Successfully converted with Aspose.Words`

**失败标志:**
- ❌ PDF 显示 "Evaluation Only. Created with Aspose.Words" 水印
- ❌ 服务器日志显示: `No Aspose license found`

---

## 故障排除

### 问题 1: Python 未找到

**错误信息:**
```
'python' 不是内部或外部命令
```

**原因:** Python 未添加到系统 PATH

**解决方案:**

**方法1: 重新安装（推荐）**
1. 卸载 Python（控制面板 → 程序 → 卸载程序）
2. 重新下载安装包
3. 安装时勾选 "Add Python to PATH"

**方法2: 手动添加到 PATH**
1. 找到 Python 安装目录，例如: `C:\Users\<用户名>\AppData\Local\Programs\Python\Python312`
2. 右键"此电脑" → 属性 → 高级系统设置 → 环境变量
3. 在"系统变量"中找到 `Path`
4. 点击"编辑"，添加 Python 目录和 Scripts 目录:
   ```
   C:\Users\<用户名>\AppData\Local\Programs\Python\Python312
   C:\Users\<用户名>\AppData\Local\Programs\Python\Python312\Scripts
   ```
5. 保存并重启命令提示符

---

### 问题 2: Aspose.Words 安装失败

**错误信息:**
```
ERROR: Could not find a version that satisfies the requirement aspose-words
```

**原因:** whl 文件路径错误或文件不存在

**解决方案:**
1. 确认 whl 文件存在
2. 使用完整路径安装:
   ```cmd
   pip install "C:\完整路径\aspose_words-25.9.0-py3-none-win_amd64.whl" --force-reinstall
   ```

---

### 问题 3: PDF 有水印

**现象:**
PDF 显示 "Evaluation Only. Created with Aspose.Words"

**原因:** 许可证文件未找到或未正确加载

**解决方案:**

**1. 检查许可证文件是否存在:**
```cmd
dir Server\1.lic
```

如果不存在，执行步骤 2。

**2. 复制许可证文件:**
```cmd
copy "Aspose.Words\python专用whl包\1.lic" "Server\1.lic"
```

**3. 重启服务器**

**4. 查看服务器日志:**
- ✅ 成功: `Found Aspose license at: ...`
- ❌ 失败: `No Aspose license found, will run in evaluation mode`

**5. 如果仍然失败，检查许可证文件是否损坏:**
```cmd
python -c "import aspose.words as aw; lic = aw.License(); lic.set_license('Server/1.lic'); print('✓ License valid')"
```

---

### 问题 4: 模块导入错误

**错误信息:**
```
ModuleNotFoundError: No module named 'aspose'
```

**原因:** Aspose.Words 未正确安装

**解决方案:**
```cmd
cd Aspose.Words\python专用whl包
pip uninstall aspose-words -y
pip install aspose_words-25.9.0-py3-none-win_amd64.whl --force-reinstall
```

---

### 问题 5: 转换超时

**错误信息:**
```
Aspose conversion timed out
```

**原因:**
- Word 文档过大（> 10MB）
- 系统资源不足
- Python 首次启动慢

**解决方案:**
1. 等待更长时间（首次转换可能需要 5-10 秒）
2. 检查文档大小，尝试较小的文档
3. 增加系统可用内存
4. 关闭其他占用资源的程序

---

### 问题 6: 端口被占用

**错误信息:**
```
Failed to bind to port 8888: Address already in use
```

**解决方案:**

**1. 查找占用端口的程序:**
```cmd
netstat -ano | findstr :8888
```

**2. 终止占用端口的进程:**
```cmd
taskkill /F /PID <进程ID>
```

**3. 或在服务器设置中修改端口**

---

### 问题 7: 权限不足

**错误信息:**
```
Access denied
Permission denied
```

**解决方案:**

**方法1: 以管理员身份运行**
- 右键 `CrossNetShareServer.exe`
- 选择"以管理员身份运行"

**方法2: 安装到用户目录**
- 将程序移动到用户目录，如:
  ```
  C:\Users\<用户名>\Documents\CrossNetShare\
  ```

---

## 高级配置

### 多用户环境部署

**服务器端（仅需一台电脑）:**
1. 安装 Python
2. 安装 Aspose.Words
3. 配置许可证
4. 启动 CrossNetShareServer.exe

**客户端（其他电脑）:**
1. 只需运行 CrossNetShareClient.exe
2. 不需要 Python 环境
3. 不需要 Aspose.Words
4. 连接到服务器 IP 即可

---

### 服务器自动启动

**方法1: 使用启动文件夹**
1. 创建 `CrossNetShareServer.exe` 的快捷方式
2. 按 `Win + R`，输入 `shell:startup`
3. 将快捷方式放入打开的文件夹

**方法2: 使用程序内置功能**
- 打开服务器设置
- 勾选"开机自动启动"

---

### 批处理脚本（便捷启动）

创建 `启动服务器.bat`:
```batch
@echo off
cd /d "%~dp0Server"
start CrossNetShareServer.exe
echo 服务器已启动
pause
```

双击运行即可启动服务器。

---

## 常见问题 FAQ

### Q1: 必须在服务器端安装 Python 吗？
**A:** 是的。服务器端需要 Python 和 Aspose.Words。客户端不需要。

### Q2: 支持哪些 Word 格式？
**A:** 支持 .doc、.docx、.rtf 格式。

### Q3: 许可证文件可以共享给其他用户吗？
**A:** 许可证文件可以在同一组织内使用，但不应公开分发。

### Q4: 能否在 Windows 7 上运行？
**A:** 不推荐。建议使用 Windows 10 或 Windows 11。

### Q5: 转换后的 PDF 质量如何？
**A:** Aspose.Words 提供高质量转换，保持原始格式和布局。

### Q6: 能否离线使用？
**A:** 可以。安装完成后无需联网，除非需要更新。

### Q7: 如何更新到新版本？
**A:** 
1. 下载新版本的 CrossNetShare
2. 替换 Server 和 Client 文件夹
3. 保留现有的 Python 和 Aspose.Words 安装
4. 确保许可证文件仍在 Server 目录

### Q8: Aspose.Words 可以用在其他项目吗？
**A:** 可以。安装后可用于任何 Python 项目的 Word 文档处理。

---

## 获取帮助

### 技术支持

- **GitHub Issues**: https://github.com/littlebabyqq2019/crossnet-share/issues
- **查看文档**: README.md、QUICKSTART.md

### 报告问题时请提供

1. 操作系统版本（Win + R → `winver`）
2. Python 版本 (`python --version`)
3. Aspose.Words 版本 (`pip show aspose-words`)
4. 完整的错误信息或服务器日志
5. 操作步骤和期望结果

### 日志位置

服务器日志显示在控制台窗口，可以：
- 右键控制台 → 选择"全选" → 按回车键复制
- 或将日志重定向到文件:
  ```cmd
  CrossNetShareServer.exe > server.log 2>&1
  ```

---

## 附录

### A. Python 安装位置

**默认安装路径:**
- 当前用户: `C:\Users\<用户名>\AppData\Local\Programs\Python\Python3xx`
- 所有用户: `C:\Program Files\Python3xx`

### B. Aspose.Words 支持的平台

| 平台 | whl 文件名 |
|------|-----------|
| Windows 64位 | `aspose_words-25.9.0-py3-none-win_amd64.whl` |
| Windows 32位 | `aspose_words-25.9.0-py3-none-win32.whl` |
| macOS Intel | `aspose_words-25.9.0-py3-none-macosx_10_14_x86_64.whl` |
| macOS ARM | `aspose_words-25.9.0-py3-none-macosx_11_0_arm64.whl` |
| Linux | `aspose_words-25.9.0-py3-none-manylinux1_x86_64.whl` |

### C. 快速安装命令汇总

```cmd
# 1. 验证 Python
python --version

# 2. 安装 Aspose.Words
cd Aspose.Words\python专用whl包
pip install aspose_words-25.9.0-py3-none-win_amd64.whl --force-reinstall

# 3. 复制许可证
copy 1.lic ..\..\Server\1.lic

# 4. 验证安装
python -c "import aspose.words; print('✓ Ready')"

# 5. 启动服务器
cd ..\..\Server
CrossNetShareServer.exe
```

---

## 版本信息

- **文档版本:** 2.0
- **软件版本:** CrossNetShare v1.3.0
- **Aspose.Words 版本:** 25.9.0
- **最后更新:** 2026-08-30

---

**祝您使用愉快！**

如有任何问题，欢迎在 GitHub 提交 Issue。
